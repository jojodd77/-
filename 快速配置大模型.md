# ⚡ 快速配置大模型 API

## 🎯 3 步完成配置

### 第一步：获取 OpenAI API Key

1. 访问：https://platform.openai.com/api-keys
2. 登录账号（如果没有，先注册）
3. 点击 "Create new secret key"
4. 复制 API Key（格式：`sk-...`）

### 第二步：创建配置文件

在项目根目录创建 `.env.local` 文件：

```bash
cd /Users/jojodd/my-tool-platform
```

然后创建文件并添加：

```env
OPENAI_API_KEY=sk-你的API密钥
OPENAI_MODEL=gpt-4o-mini
```

**方法1：使用终端**
```bash
cat > .env.local << 'EOF'
OPENAI_API_KEY=sk-你的API密钥
OPENAI_MODEL=gpt-4o-mini
EOF
```

**方法2：使用编辑器**
```bash
# 使用 VS Code
code .env.local

# 或使用 nano
nano .env.local
```

### 第三步：重启服务器

```bash
# 停止当前服务器（如果正在运行）
# 按 Ctrl + C

# 重新启动
npm run dev
```

---

## ✅ 验证配置

配置成功后：

1. 访问：http://localhost:3001
2. 进入"发音修正"页面
3. 输入测试文本：`图中哪个光路图是正确的？`
4. 点击"判断并修正"
5. 应该能看到使用真实大模型的修正结果

---

## 💡 提示

- **模型选择**：
  - `gpt-4o-mini`：推荐，性价比高
  - `gpt-4`：最准确，但较贵
  - `gpt-3.5-turbo`：平衡选择

- **费用**：使用 gpt-4o-mini，每次修正约 $0.0003-0.0006

- **安全**：`.env.local` 已在 `.gitignore` 中，不会提交到 Git

---

## 📝 示例配置

`.env.local` 文件内容：

```env
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_MODEL=gpt-4o-mini
```

---

**配置完成后，平台就可以使用真实的大模型进行发音修正了！** 🚀



