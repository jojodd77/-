# ⏱️ 安装时间说明

## 📊 正常安装时间

### 使用默认源（registry.npmjs.org）
- **首次安装**：5-15 分钟
- **网络较慢时**：可能 20-30 分钟

### 使用国内镜像（registry.npmmirror.com）
- **首次安装**：2-5 分钟
- **网络正常时**：通常 3 分钟内完成

---

## 🔍 如何判断安装是否完成？

### 方法一：查看终端输出

**安装完成的标志**：
```
added 250 packages, and audited 251 packages in 2m
```

或者：
```
found 0 vulnerabilities
```

**如果看到这些，说明安装已完成！** ✅

### 方法二：检查文件

```bash
# 检查 node_modules 目录大小
du -sh node_modules

# 正常应该显示 100-300MB
```

### 方法三：检查包数量

```bash
ls node_modules | wc -l

# 正常应该显示 200-300 个包
```

---

## ⚠️ 如果安装时间过长

### 超过 15 分钟还没完成？

1. **检查网络连接**
   ```bash
   ping registry.npmmirror.com
   ```

2. **使用镜像源重新安装**
   ```bash
   # 取消当前安装（Ctrl + C）
   # 然后使用镜像
   npm install --registry=https://registry.npmmirror.com
   ```

3. **清除缓存重试**
   ```bash
   npm cache clean --force
   npm install --registry=https://registry.npmmirror.com
   ```

---

## 🚀 加速安装的方法

### 1. 使用国内镜像（最快）

```bash
npm config set registry https://registry.npmmirror.com
npm install
```

### 2. 使用 yarn（通常更快）

```bash
npm install -g yarn
yarn install
```

### 3. 使用 pnpm（最快）

```bash
npm install -g pnpm
pnpm install
```

---

## 📈 安装进度判断

### 正常情况下的进度

1. **开始阶段**（0-1分钟）
   - 解析依赖关系
   - 显示 `npm WARN` 警告（正常）

2. **下载阶段**（1-5分钟）
   - 下载包文件
   - 显示下载进度

3. **安装阶段**（最后1分钟）
   - 解压和安装
   - 显示 `added XXX packages`

---

## 💡 建议

### 如果当前安装很慢

1. **等待**：如果已经安装了 5-10 分钟，再等一会儿
2. **检查**：看看终端是否还在输出（说明还在安装）
3. **取消重来**：如果超过 20 分钟，可以取消（Ctrl + C）然后用镜像重装

### 使用镜像的命令

```bash
# 取消当前安装
# 按 Ctrl + C

# 使用镜像重新安装
npm install --registry=https://registry.npmmirror.com
```

---

## ✅ 安装完成后

看到 `added XXX packages` 后，就可以启动：

```bash
npm run dev
```

---

**正常安装时间：2-5 分钟（使用镜像）或 5-15 分钟（默认源）**

如果超过 20 分钟还没完成，建议取消并使用镜像重新安装。



