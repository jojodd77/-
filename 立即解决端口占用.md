# 🚨 立即解决端口 3001 占用问题

## 问题

端口 3001 仍被占用，无法启动开发服务器。

## ✅ 快速解决方案

### 方法 1：使用一行命令（推荐）

在你的终端中执行：

```bash
cd /Users/jojodd/my-tool-platform && lsof -ti :3001 | xargs kill -9 2>/dev/null; pkill -9 -f "next dev" 2>/dev/null; pkill -9 node 2>/dev/null; sleep 3; rm -rf .next; npm run dev
```

这个命令会：
1. 停止占用端口 3001 的进程
2. 停止所有 Next.js 进程
3. 停止所有 node 进程
4. 等待进程完全停止
5. 清理构建缓存
6. 启动开发服务器

### 方法 2：分步执行（如果方法1不行）

```bash
# 步骤 1: 进入项目目录
cd /Users/jojodd/my-tool-platform

# 步骤 2: 强制停止所有相关进程
lsof -ti :3001 | xargs kill -9 2>/dev/null
pkill -9 -f "next dev" 2>/dev/null
pkill -9 node 2>/dev/null

# 步骤 3: 等待进程停止
sleep 5

# 步骤 4: 验证端口已释放
lsof -i :3001

# 如果上面没有输出，说明端口已释放

# 步骤 5: 清理缓存
rm -rf .next

# 步骤 6: 启动服务器
npm run dev
```

### 方法 3：使用 sudo 权限（如果方法2不行）

```bash
cd /Users/jojodd/my-tool-platform

# 使用 sudo 强制停止
sudo lsof -ti :3001 | xargs sudo kill -9 2>/dev/null
sudo pkill -9 -f "next dev" 2>/dev/null
sudo pkill -9 node 2>/dev/null

sleep 5

# 验证
lsof -i :3001

# 如果端口已释放，启动服务器
rm -rf .next
npm run dev
```

### 方法 4：使用其他端口（临时方案）

如果无法停止占用端口的进程，可以临时使用其他端口：

```bash
cd /Users/jojodd/my-tool-platform
npm run dev -- -p 3002
```

然后访问 http://localhost:3002

## 🔍 查找占用端口的进程

如果你想查看是什么进程占用了端口：

```bash
# 查看占用端口的进程详情
lsof -i :3001

# 或者
netstat -an | grep 3001
```

## 🐛 如果仍然无法解决

### 选项 1：重启电脑

这是最简单但最彻底的方法：
1. 保存所有工作
2. 重启电脑
3. 重启后运行 `npm run dev`

### 选项 2：修改端口号

修改 `package.json` 中的端口号：

```json
{
  "scripts": {
    "dev": "next dev -p 3002"
  }
}
```

然后运行：
```bash
npm run dev
```

访问 http://localhost:3002

## ✅ 验证修复

启动成功后，你应该看到：

```
▲ Next.js 14.2.5
- Local:        http://localhost:3001
- Ready in X.Xs
```

然后访问 http://localhost:3001 应该可以正常打开了。

## 💡 预防措施

为了避免将来出现这个问题：

1. **正确停止服务器**
   - 使用 `Ctrl+C` 停止，不要直接关闭终端窗口

2. **启动前检查端口**
   ```bash
   lsof -i :3001
   ```
   如果没有输出，说明端口可用

3. **使用脚本自动处理**
   ```bash
   # 创建一个启动脚本
   echo 'lsof -ti :3001 | xargs kill -9 2>/dev/null; npm run dev' > start.sh
   chmod +x start.sh
   ./start.sh
   ```



