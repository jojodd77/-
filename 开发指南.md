# 开发指南

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 配置环境变量

复制 `.env.example` 为 `.env.local`：

```bash
cp .env.example .env.local
```

然后编辑 `.env.local` 填写你的配置。

### 3. 运行开发服务器

```bash
npm run dev
```

访问 [http://localhost:3000](http://localhost:3000)

## 📁 项目结构

```
my-tool-platform/
├── app/                    # Next.js App Router
│   ├── api/               # API 路由
│   ├── (routes)/          # 页面路由（可选分组）
│   ├── layout.tsx         # 根布局
│   ├── page.tsx           # 首页
│   └── globals.css        # 全局样式
├── components/            # React 组件
├── lib/                   # 工具函数和配置
├── types/                 # TypeScript 类型
├── public/                # 静态资源
└── ...
```

## 🛠️ 开发规范

### 代码风格

- 使用 TypeScript 编写所有代码
- 遵循 ESLint 规则
- 使用 Prettier 格式化代码（推荐安装 Prettier 扩展）

### 组件开发

- 组件放在 `components/` 目录
- 使用函数式组件和 Hooks
- 为 Props 定义明确的类型
- 组件名使用 PascalCase

### API 路由

- API 路由放在 `app/api/` 目录
- 使用 Route Handlers（Next.js 13+）
- 返回 JSON 使用 `NextResponse.json()`

### 样式

- 使用 Tailwind CSS 工具类
- 全局样式在 `app/globals.css`
- 组件样式使用 className

## 📝 Git 工作流

### 分支策略

- `main`: 生产环境代码
- `develop`: 开发分支（可选）
- `feature/功能名`: 新功能开发
- `fix/问题描述`: Bug 修复

### 提交规范

使用 Conventional Commits：

```bash
feat: 添加新功能
fix: 修复 bug
docs: 更新文档
style: 代码格式调整
refactor: 代码重构
test: 添加测试
chore: 构建/工具相关
```

### 示例

```bash
# 创建功能分支
git checkout -b feature/user-authentication

# 开发并提交
git add .
git commit -m "feat: 添加用户登录功能"

# 推送到远程
git push origin feature/user-authentication
```

## 🧪 测试

### 运行 Lint

```bash
npm run lint
```

### 构建检查

```bash
npm run build
```

## 📦 部署

### Vercel 部署（推荐）

1. 将代码推送到 GitHub
2. 在 [Vercel](https://vercel.com) 导入项目
3. 配置环境变量
4. 自动部署

### 其他平台

- Netlify
- Railway
- 自建服务器

## 🔗 相关资源

- [Next.js 文档](https://nextjs.org/docs)
- [TypeScript 文档](https://www.typescriptlang.org/docs)
- [Tailwind CSS 文档](https://tailwindcss.com/docs)
- [React 文档](https://react.dev)

## ❓ 常见问题

### Q: 如何添加新的页面？

在 `app/` 目录下创建新文件夹，例如 `app/about/page.tsx`

### Q: 如何创建 API 端点？

在 `app/api/` 目录下创建路由文件，例如 `app/api/users/route.ts`

### Q: 如何使用环境变量？

在 `.env.local` 中定义，使用 `process.env.NEXT_PUBLIC_*` 访问（客户端可见）

