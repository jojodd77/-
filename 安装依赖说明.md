# ⏱️ 安装依赖说明

## 为什么安装依赖需要时间？

### 1. 需要下载的包较多

Next.js 项目包含以下依赖：

**生产依赖**：
- react (18.2.0)
- react-dom (18.2.0)
- next (14.2.5)

**开发依赖**：
- typescript (5.5.3)
- @types/node, @types/react, @types/react-dom
- tailwindcss, postcss, autoprefixer
- eslint, eslint-config-next

**总计**：大约需要下载 200+ 个包（包括所有依赖的依赖）

### 2. 网络速度

- 如果使用默认 npm 源（registry.npmjs.org），可能较慢
- 国内用户建议使用镜像源

### 3. 首次安装

- 需要下载所有包
- 后续安装会使用缓存，会快很多

---

## 🚀 加速方法

### 方法一：使用国内镜像（推荐）

```bash
# 设置淘宝镜像
npm config set registry https://registry.npmmirror.com

# 然后安装
npm install
```

### 方法二：使用 yarn（通常更快）

```bash
# 安装 yarn（如果还没有）
npm install -g yarn

# 使用 yarn 安装
yarn install
```

### 方法三：使用 pnpm（最快）

```bash
# 安装 pnpm
npm install -g pnpm

# 使用 pnpm 安装
pnpm install
```

---

## ⏱️ 预计时间

- **正常网络**：3-5 分钟
- **使用镜像**：1-3 分钟
- **使用 yarn/pnpm**：1-2 分钟

---

## 📊 检查安装进度

安装过程中会显示：
- 下载进度
- 已安装的包数量
- 剩余时间（如果有）

---

## ✅ 安装成功的标志

看到类似这样的输出：

```
added 250 packages, and audited 251 packages in 2m
```

或者：

```
✓ Installed dependencies
```

---

## 🔍 如果卡住了

### 检查是否真的在运行

```bash
# 查看网络活动（Mac）
# 打开"活动监视器"，查看 Node 进程的 CPU 和网络使用
```

### 如果超过 10 分钟还没完成

1. **按 Ctrl + C 取消**
2. **清除缓存重试**：
   ```bash
   npm cache clean --force
   npm install
   ```

3. **使用镜像源**：
   ```bash
   npm config set registry https://registry.npmmirror.com
   npm install
   ```

---

## 💡 建议

对于国内用户，**强烈建议使用镜像源**：

```bash
# 永久设置镜像
npm config set registry https://registry.npmmirror.com

# 或者只对当前项目设置
npm install --registry=https://registry.npmmirror.com
```

---

**耐心等待，安装完成后就可以启动开发服务器了！** ⏳



